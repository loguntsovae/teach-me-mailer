# Example environment for the client service
# URL of the mailer send endpoint (include full path to /api/v1/send)
MAILER_SEND_URL=http://localhost:8000/api/v1/send

# API key to use when calling the mailer service (header X-API-Key)
MAILER_API_KEY=sk_test_examplechangeme

# Request timeout in seconds
MAILER_TIMEOUT=5
